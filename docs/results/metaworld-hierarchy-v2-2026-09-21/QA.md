# 图表核对

- Python 绘图，183 × 194 mm，600 dpi PNG，并导出可编辑 SVG/PDF。
- 已查看最终 PNG：六个面板、图例和缺报标记可读，无裁切或遮挡。
- 十二个案例均显示；两次 GPT 请求超时保持 PARTIAL，缺失 token 显示 N/A。
- Jev 完整组六局可显示 Wilson 区间；GPT 不完整总体及受影响任务不显示区间。
- 171 项数据记录核对通过，六局 GPT 的逐请求落盘日志均与汇总一致。
- 静态绘图检查 13 PASS、1 WARN、0 FAIL。WARN 为未导出 TIFF；此报告要求 PNG/PDF/SVG，没有 TIFF 投稿要求。
- GPT 新批次整批替换主图，旧批次单独保留；没有排除运行异常、选择最优重复或填造用量。
